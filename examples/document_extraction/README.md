# Document Extraction Example

This example shows how to use the framework to model document extraction with explicit identity, evidence, invariant checks, evaluation runs, and run comparability.

Run:

```bash
python examples/document_extraction/run_example.py
```
