# ADR-003: Worldview policy is typed and operational

## Decision
Model worldview as `WorldViewProfile` and apply it at runtime/evaluation.

## Rationale
Trust hierarchy, ambiguity handling, and comparability behavior must be enforceable, not informal.

## Consequences
Comparability and classification behavior use worldview policy explicitly.
