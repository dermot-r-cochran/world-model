# World view

A world view is a typed policy artifact that governs interpretation and operational decisions over the world model.

`WorldViewProfile` includes:
- trust hierarchy
- ambiguity policy
- fallback alignment policy
- comparability policy
- escalation policy
- accountability mapping (mismatch class to owner team)

World view is operationalized in runtime/evaluation decisions and never treated as a docs-only concept.
